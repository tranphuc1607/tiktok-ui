import style from"./Search.module.scss"
import classNames from "classnames/bind";

const cx = classNames.bind(style)

function Search() {
    return ( <p1>search page</p1> );
}

export default Search;