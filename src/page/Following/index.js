import style from './Following.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(style);

function Following() {
    return <div className="w-full">Following page</div>;
}

export default Following;
